function firstDuplicate(array) {
    var myArray = [1, 4, 2, 5, 2, 4];
    let count = 0;
    for(let j = 0; j < myArray.length; j++) {
        
    for(let i = 0; i < myArray.length; i++) {
    //let reggy = new RegExp(`\w${myArray[i]}\w`, 'gmi');
    if (myArray[j] === myArray[i]) {
        count += 1;

    } else {
        
    }
    }}
    //console.log(count)
}
//ugh
